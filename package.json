{
  "name": "mubiflix_api",
  "version": "1.0.0",
  "description": "Api for mubiflix",
  "main": "index.js",
  "scripts": {
    "build": " npm run clear && tsc && cp src/routes/* dist/routes/",
    "clear": "rm -rf dist",
    "cleaninstall": "rm -rf node_modules package-lock.json && npm install",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "axios",
    "express",
    "jsdoc",
    "swagger"
  ],
  "author": "basierraferrer@gmail.com",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "express": "^4.18.2",
    "swagger-ui-express": "^5.0.0",
    "yaml": "^2.3.1"
  }
}